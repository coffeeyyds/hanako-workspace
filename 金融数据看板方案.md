# 金融全景数据看板 — 方案设计

> 目标：一个可以实时纵览全球金融动向的个人数据看板。覆盖中国全品类金融市场 + 国际市场（股市、债市、币圈、预测市场）+ AI 最新动态。一手数据源优先，支持翻墙环境。

---

## 一、项目定位

**"Bloomberg Terminal 的个人开源替代 + AI 洞察层"**

不只是一个 K 线看盘软件。是全品类金融数据聚合器 + 仪表盘 + AI 辅助分析引擎。类比：把 TradingView 的图表能力、东方财富的数据广度、Bloomberg 的信息密度、再加上 AI 动态追踪，融合成一个可自行部署的私有看板。

核心原则：
- **一手数据源优先**：直连交易所/官方接口，不经过二手聚合商
- **全品类覆盖**：股票、债券、期货、保险、币、宏观、预测市场，缺一不可
- **中国深度**：A 股短线指标、涨停板、龙虎榜、热门板块竞品级呈现
- **实时性**：盘中秒级更新，盘后分钟级更新
- **自部署**：跑在自己电脑上（Windows），Docker Desktop 本地运行，数据自主可控

---

## 二、数据源全景图

### 2.1 A 股（核心模块）

所有国内数据的主力获取方式：**AKShare**（开源 Python 库，2000+ 接口，零 API Key）

#### 实时行情
| 数据类型 | 获取方式 | 频率 | 备注 |
|---------|---------|------|------|
| 个股实时行情 | AKShare `stock_zh_a_spot_em` / 新浪 `hq.sinajs.cn` | 3秒轮询 | 东方财富/新浪免费接口，无需 Key |
| 指数实时行情 | AKShare `stock_zh_index_spot_em` | 3秒轮询 | 上证、深证、创业、科创 |
| 分时数据 | AKShare `stock_zh_a_minute` | 盘中每分钟 | 当日分时线 |
| 历史日线 | AKShare + baostock | 盘后更新 | 复权数据 |

#### 涨停板/龙虎榜/热门板块
| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| 涨停板池 | AKShare `stock_zt_pool_em` | 当日涨停股、连板数、封板时间、炸板 |
| 跌停板池 | AKShare `stock_dt_pool_em` | |
| 龙虎榜 | AKShare `stock_lhb_detail_em` | 营业部、席位、买卖金额 |
| 热门板块 | AKShare `stock_board_concept_hist_em` | 概念板块涨跌幅、资金流向 |
| 行业板块 | AKShare `stock_board_industry_cons_em` | |
| 资金流向 | AKShare `stock_individual_fund_flow` | 个股主力资金 |

#### 技术指标
| 指标类型 | 计算方式 | 备注 |
|---------|---------|------|
| MA/EMA/SMA | pandas-ta | 均线系统，`pip install` 即装，130+ 指标 |
| MACD/KDJ/RSI/BOLL | pandas-ta | 经典指标，一行 `df.ta.macd()` |
| 成交量指标 | OBV、VWAP | |
| 自研指标 | 可自定义 | 基于原始数据计算 |

#### 短线专用数据
| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| 竞价数据 | AKShare `stock_zh_a_bid_em` | 集合竞价 |
| 分笔成交 | 需要券商/通达信接口 | 高阶需求，后期考虑 |
| 融资融券 | AKShare `stock_margin_detail_sse` | |

> **AKShare 覆盖范围**：股票、指数、基金、期货、期权、债券、外汇、宏观、新闻、情绪指标等全覆盖。是整个国内数据的核心引擎。

### 2.2 债券市场

| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| 国债收益率曲线 | AKShare `bond_china_yield` | 中国债券信息网数据 |
| LPR 利率 | AKShare `macro_china_lpr` | |
| Shibor 利率 | AKShare `rate_interbank` | |
| 可转债 | AKShare `bond_cb_jsl` | 集思录可转债数据 |
| 企业债 | AKShare `bond_china_close_return` | |
| 美债收益率 | yfinance / FRED API | 2年/10年/30年 |

### 2.3 期货市场

| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| 国内期货实时 | AKShare `futures_zh_spot` | 上期所/大商所/郑商所/中金所 |
| 国内期货历史 | AKShare `futures_zh_daily` | |
| 国际期货 | yfinance（COMEX/ICE/NYMEX） | 黄金、原油、铜、天然气 |
| 期权数据 | AKShare `option_*` | 50ETF/300ETF 期权 |

### 2.4 宏观数据

| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| CPI/PPI | AKShare `macro_china_cpi` / `macro_china_ppi` | |
| PMI | AKShare `macro_china_pmi` | |
| M2/社融 | AKShare `macro_china_money_supply` | |
| GDP | AKShare `macro_china_gdp` | |
| 央行公开市场操作 | AKShare `macro_china_repo` | 逆回购、MLF |
| 外汇储备 | AKShare | |

### 2.5 汇率

| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| 人民币汇率中间价 | AKShare `fx_spot_quote` | |
| 离岸人民币 | AKShare / yfinance (CNH=X) | |
| 主要货币对 | yfinance / ExchangeRate-API | 免费额度可满足 |

### 2.6 美股/全球股市（详细）

核心工具：**yfinance**。基于 Yahoo Finance 公开 API，免费、无需 Key、无频率限制（合理使用即可）。

```python
import yfinance as yf

# 单只股票
msft = yf.Ticker("MSFT")
hist = msft.history(period="6mo")          # OHLCV 日线
info = msft.info                             # 基本面（市值、PE、行业等）

# 批量下载
from yfinance import download
data = download(["AAPL", "GOOGL", "TSLA"], period="1y")

# 指数
sp500 = download("^GSPC", period="5d")       # 标普500
nasdaq = download("^IXIC", period="5d")      # 纳斯达克
dji = download("^DJI", period="5d")          # 道琼斯
vix = download("^VIX", period="5d")          # VIX 恐慌指数

# 货币对
cny = download("CNH=X", period="1mo")         # 美元/离岸人民币
```

| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| 美股实时行情 | yfinance（15 分钟延迟） | 免费无限调用，足够看盘 |
| 美股历史数据 | yfinance `history(period="5y")` | 日/周/月线，含分红复权 |
| ETF 数据 | yfinance | SPY/QQQ/IWM/ARKK 等 |
| 基本面数据 | yfinance `.info` | PE、市值、股息率、行业分类 |
| 财报日历 | yfinance `.calendar` / `.earnings_dates` | |
| 港股 | AKShare `stock_hk_spot_em` | 实时行情 |
| 日经/富时/DAX | yfinance (^N225, ^FTSE, ^GDAXI) | |

**备用方案**：
- **Alpha Vantage**：`https://www.alphavantage.co/query?function=TIME_SERIES_DAILY&symbol=MSFT&apikey=XXX`，需要免费 API Key，每天 25 次（免费层）
- **Finnhub**：`https://finnhub.io/api/v1/quote?symbol=AAPL&token=XXX`，免费 Key，60次/分钟

### 2.7 加密货币（详细）

**方案一（主力）：Binance WebSocket 实时行情**

```python
# 使用 python-binance 库
# pip install python-binance
from binance import ThreadedWebsocketManager

twm = ThreadedWebsocketManager(api_key="", api_secret="")

def handle_ticker(msg):
    # msg['c'] = 最新价, msg['h'] = 24h最高, msg['l'] = 24h最低
    # msg['v'] = 成交量, msg['P'] = 涨跌幅
    print(f"{msg['s']}: ${msg['c']} ({msg['P']}%)")

twm.start()
twm.start_symbol_ticker_socket(callback=handle_ticker, symbol='BTCUSDT')
twm.start_symbol_ticker_socket(callback=handle_ticker, symbol='ETHUSDT')
# 可同时订阅几十个交易对
```

也可以直接用 CCXT 统一接口（同时支持 Binance/OKX/Bybit 等多个交易所）：

```python
# pip install ccxt
import ccxt

binance = ccxt.binance()

# REST 方式获取实时 ticker
ticker = binance.fetch_ticker('BTC/USDT')
# ticker['last'], ticker['change'], ticker['volume']

# WebSocket 实时行情
async def watch_tickers():
    exchange = ccxt.binance({'enableRateLimit': True})
    while True:
        tickers = await exchange.watch_tickers(['BTC/USDT', 'ETH/USDT'])
        for symbol, t in tickers.items():
            print(f"{symbol}: ${t['last']}")
```

**方案二（补充）：CoinGecko REST API**

```python
# pip install pycoingecko
from pycoingecko import CoinGeckoAPI

cg = CoinGeckoAPI()

# 加密货币列表（市值排行）
cg.get_coins_markets(vs_currency='usd', order='market_cap_desc', per_page=100)

# 单个币种价格（同时查多个）
cg.get_price(ids='bitcoin,ethereum,solana', vs_currencies='usd,cny',
             include_24hr_change='true', include_market_cap='true')

# 历史数据
cg.get_coin_market_chart_by_id(id='bitcoin', vs_currency='usd', days='30')
```

| 数据类型 | 获取方式 | 备注 |
|---------|---------|------|
| 实时行情 | Binance WS（主力） | 毫秒级延迟，免费，无需 API Key（只读） |
| 币种补充列表 | CoinGecko REST | 30 次/分钟，免费层无需 Key |
| DeFi TVL | DeFiLlama API `https://api.llama.fi/protocols` | 免费，返回所有协议 TVL |
| 恐惧贪婪指数 | `https://api.alternative.me/fng/?limit=1` | 免费，返回数值和分类 |
| 链上 BTC 指标 | `https://api.blockchain.info/stats` | 免费，哈希率/难度/交易量 |

### 2.8 Polymarket / 预测市场（详细）

Polymarket 提供两层 API：

| API 层 | 地址 | 用途 |
|-------|------|------|
| **Gamma API** | `https://gamma-api.polymarket.com` | 市场列表、事件元数据、按标签筛选 |
| **CLOB API** | `https://clob.polymarket.com` | 订单簿、实时价格、下单交易 |

Gamma API 示例（只需要市场数据，不要交易）：

```python
import requests

# 获取热门市场列表
resp = requests.get(
    "https://gamma-api.polymarket.com/markets",
    params={"limit": 50, "order": "volume24hr", "ascending": False},
    proxies={"https": "http://127.0.0.1:7890"}  # 走翻墙代理
)
markets = resp.json()
# 每个 market 包含: question, outcomes, outcomePrices, volume24hr

# 获取特定事件的子市场
resp = requests.get(
    "https://gamma-api.polymarket.com/events",
    params={
        "tag": "politics",          # 标签: politics, crypto, sports, science, etc.
        "limit": 20,
        "order": "volume24hr",
        "ascending": False
    },
    proxies={"https": "http://127.0.0.1:7890"}
)
events = resp.json()
```

**如果没有 API Key 或 API 挂了**，直接用 Playwright 浏览器抓取页面：

```python
# Playwright 兜底方案
async def scrape_polymarket_homepage():
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)
        page = await browser.new_page()
        # 走代理
        context = await browser.new_context(
            proxy={"server": "http://127.0.0.1:7890"}
        )
        page = await context.new_page()
        await page.goto("https://polymarket.com/")
        await page.wait_for_selector("a[href^='/event/']")
        content = await page.content()
        # 解析 HTML 提取市场标题和概率
        await browser.close()
        return content
```

### 2.9 经济日历

| 数据源 | 获取方式 | URL | 备注 |
|-------|---------|-----|------|
| Investing.com | Playwright 抓取 | `investing.com/economic-calendar` | 最全，含中国数据 |
| Forex Factory | 网页解析 | `forexfactory.com/calendar` | 每周重要事件 |
| FRED (美联储经济数据) | `fredapi` Python 库 | `research.stlouisfed.org` | 需要免费 API Key，不限量 |

```python
# FRED API 示例
# pip install fredapi
from fredapi import Fred
fred = Fred(api_key="注册免费获取")

# 美国 GDP, CPI, 失业率, 10年国债收益率
gdp = fred.get_series('GDP')
cpi = fred.get_series('CPIAUCSL')
unemployment = fred.get_series('UNRATE')
t10y = fred.get_series('DGS10')  # 10年期国债收益率
t2y = fred.get_series('DGS2')    # 2年期
```

---

## 三、AI 最新动向（详细）

### 3.1 学术论文

| 数据源 | 获取方式 | Python 示例 | 更新频率 |
|-------|---------|------------|---------|
| arXiv | API: `http://export.arxiv.org/api/query?search_query=cat:cs.AI&sortBy=submittedDate&max_results=20` | `pip install arxiv` → `arxiv.Search(query="cat:cs.AI", max_results=20)` | 每日新增 |
| Papers With Code | API: `https://paperswithcode.com/api/v1/papers/` | `requests.get()` 带分页 | 每日 |
| Hugging Face Daily | 网页抓取 `https://huggingface.co/papers` | Playwright + BeautifulSoup | 每日精选 |
| GitHub AI Trending | API: `https://api.github.com/search/repositories?q=topic:ai&sort=updated` | `requests.get()` 无需 Key（60次/小时） | 实时 |

```python
# arXiv 论文抄底
import arxiv

search = arxiv.Search(
    query="cat:cs.AI OR cat:cs.CL OR cat:cs.LG",
    max_results=30,
    sort_by=arxiv.SortCriterion.SubmittedDate
)
for paper in search.results():
    print(f"{paper.title} | {paper.published}")
    print(f"  {paper.summary[:200]}...")
```

### 3.2 行业新闻（RSS 一键聚合）

| 数据源 | RSS URL | 语言 |
|-------|---------|------|
| The Verge AI | `https://www.theverge.com/ai-artificial-intelligence/rss/index.xml` | EN |
| TechCrunch AI | `https://techcrunch.com/category/artificial-intelligence/feed/` | EN |
| Ars Technica AI | `https://feeds.arstechnica.com/arstechnica/ai` | EN |
| VentureBeat AI | `https://venturebeat.com/category/ai/feed/` | EN |
| MIT Tech Review AI | `https://www.technologyreview.com/topic/artificial-intelligence/feed` | EN |
| 机器之心 | `https://www.jiqizhixin.com/rss` | CN |
| 量子位 | `https://www.qbitai.com/feed` | CN |
| 36氪 AI | `https://36kr.com/feed?cid=298` | CN |
| Hacker News AI | 搜索 API: `https://hn.algolia.com/api/v1/search?query=AI+OR+LLM+OR+GPT&tags=story&hitsPerPage=20` | EN |

```python
# RSS 聚合读取
# pip install feedparser
import feedparser

urls = [
    "https://www.jiqizhixin.com/rss",
    "https://www.theverge.com/ai-artificial-intelligence/rss/index.xml",
]
for url in urls:
    feed = feedparser.parse(url)
    for entry in feed.entries[:5]:
        print(f"[{feed.feed.title}] {entry.title}")
        print(f"  {entry.link}")
```

### 3.3 大模型 API 价格跟踪

通过定期抓取各厂商定价页面来追踪：

| 厂商 | 价格页 | 抓取方式 |
|-----|-------|---------|
| OpenAI | `https://openai.com/api/pricing/` | Playwright（页面动态渲染） |
| Anthropic | `https://www.anthropic.com/pricing` | Playwright |
| Google AI | `https://ai.google.dev/pricing` | 简单 HTTP 请求 |
| DeepSeek | `https://platform.deepseek.com/api-docs/pricing` | 简单 HTTP |
| 阿里百炼 | `https://help.aliyun.com/zh/model-studio/getting-started/models` | Playwright |
| SiliconFlow | `https://siliconflow.cn/pricing` | Playwright |

### 3.4 舆情/社交媒体

| 数据源 | 获取方式 |
|-------|---------|
| Reddit r/MachineLearning | `https://www.reddit.com/r/MachineLearning/hot.json` |
| Reddit r/singularity | `https://www.reddit.com/r/singularity/hot.json` |
| X (Twitter) AI 圈 | Browser tool 抓取，或用 Nitter 镜像 RSS |

> AI 模块的核心策略：**RSS/API 自动抓取 + LLM 摘要提炼**。每天几百条信息没人能看完，让 LLM（DeepSeek/GPT）自动提炼出 10 条最重要的，推送到看板首页。

---

## 四、技术架构

### 4.1 总览

```
┌──────────────────────────────────────────────────────┐
│                    前端展示层 (React)                   │
│  TradingView Charts │ ECharts │ Grid 看板 │ 深色主题     │
├──────────────────────────────────────────────────────┤
│                   API 网关层 (FastAPI)                  │
│  REST API │ WebSocket 推送 │ 数据聚合 │ 缓存管理        │
├──────────────────────────────────────────────────────┤
│                   数据存储层                            │
│  PostgreSQL (主库) │ Redis (缓存/实时) │ TimescaleDB    │
├──────────────────────────────────────────────────────┤
│                   数据采集层                            │
│  AKShare │ yfinance │ CoinGecko │ Binance WS │ RSS   │
│  Celery Beat 定时调度 │ 翻墙代理路由                    │
└──────────────────────────────────────────────────────┘
```

### 4.2 技术栈选型

| 层级 | 技术 | 理由 |
|------|------|------|
| **前端框架** | React 18 + TypeScript | 生态最全，TradingView 官方有 React 绑定 |
| **图表** | TradingView `lightweight-charts` + ECharts | TV 做K线，ECharts 做饼图/热力图/关系图 |
| **看板布局** | `react-grid-layout` | 拖拽排列、可自定义、响应式 |
| **UI 组件** | Tailwind CSS + Radix UI | 深色主题优先，金融终端风格 |
| **后端** | Python FastAPI | 与 AKShare/pandas 生态天然兼容 |
| **WebSocket** | FastAPI WebSocket + Redis Pub/Sub | 实时推送行情 |
| **任务调度** | Celery + Celery Beat + Redis Broker | 定时采集数据 |
| **主数据库** | PostgreSQL 16 + TimescaleDB 扩展 | 时序数据压缩、超表自动分区 |
| **缓存** | Redis | 实时数据缓存、WebSocket 消息队列 |
| **采集引擎** | AKShare + yfinance + aiohttp | 异步爬取 |
| **翻墙代理** | 采集层配置 HTTP_PROXY（V2Ray/Clash 本地端口） | 按域名分流（国内直连、国外走代理） |
| **部署** | Docker Compose | 一键部署 |

### 4.3 数据流

```
[定时任务 Celery Beat]
  ├── 盘前 9:00: 更新基本面、涨停板预告
  ├── 盘中 9:30-15:00: 每3秒采集实时行情 → Redis → WebSocket 推前端
  ├── 盘后 15:30: 更新日K、龙虎榜、技术指标计算
  ├── 每5分钟: 币圈行情（Binance WS 直推，不轮询）
  ├── 每小时: AI 资讯 RSS 抓取 + AI 摘要生成
  └── 每日凌晨: 宏观经济数据更新、美债收益率

[翻墙路由]
  国内数据 (AKShare) → 直连
  国外数据 (yfinance/CoinGecko/Polymarket) → HTTP_PROXY=socks5://127.0.0.1:7890
```

### 4.4 数据采集层详细设计

```python
# 采集器目录结构
collectors/
├── __init__.py
├── base.py            # 基类
├── domestic/
│   ├── a_stock.py     # A股 (AKShare)
│   ├── bond.py        # 债券
│   ├── futures.py     # 期货
│   ├── macro.py       # 宏观
│   └── fx.py          # 汇率
├── international/
│   ├── us_stock.py    # 美股 (yfinance)
│   ├── crypto.py      # 加密货币 (CCXT/Binance WS)
│   ├── polymarket.py  # 预测市场
│   └── commodity.py   # 大宗商品
├── news/
│   ├── rss_feed.py    # RSS 抓取
│   └── ai_summary.py  # AI 摘要（调用 LLM）
└── proxy.py           # 代理管理
```

### 4.5 数据库设计概要

```sql
-- 核心表设计
market_data_realtime   -- 实时行情 (TimescaleDB 超表，按时间自动分区)
  id, symbol, name, price, change_pct, volume, turnover, timestamp

market_data_daily      -- 日线数据 (TimescaleDB)
  id, symbol, date, open, high, low, close, volume, turnover

limit_up_pool          -- 涨停板池
  date, symbol, name, limit_up_time, consecutive_days, ...

dragon_tiger_list      -- 龙虎榜
  date, symbol, name, buy_amount, sell_amount, reason, ...

hot_sectors             -- 热门板块
  date, sector_name, change_pct, leading_stock, ...

macro_indicators       -- 宏观指标
  indicator_name, value, date

ai_news                -- AI 资讯
  id, source, title, url, summary, published_at, created_at

crypto_tickers         -- 加密货币行情
polymarket_markets     -- Polymarket 市场数据
```

---

## 五、功能模块设计

### 5.1 首页总览（Dashboard）

- 全球市场热力地图（TradingView `lightweight-charts` 做不到热力图，用 ECharts）
- 核心指数一览卡片（上证、深证、创业、科创 50、恒生、标普 500、纳斯达克、日经、富时）
- 自选股 Watch List
- 市场情绪仪表盘（涨跌比、涨停/跌停数、成交量）
- 资金流向概览（北向、南向、主力）

### 5.2 A 股深度模块

**实时行情台**
- 个股 K 线图（日/周/月/分钟，TradingView 图表）
- MA/MACD/KDJ/RSI/BOLL 叠加显示
- 分时图（盘中）
- 盘口深度（五档/十档）

**涨停板追踪**
- 今日涨停列表（连板数、封板时间、炸板标记）
- 跌停列表
- 连板梯队（2连板→N连板分布图）
- 首板挖掘

**龙虎榜**
- 龙虎榜明细（买卖席位、游资识别）
- 机构/游资动向统计

**热门板块轮动**
- 概念板块热力图
- 板块资金流向排行
- 板块内个股联动

**技术选股**
- 自定义指标筛选（金叉/死叉/超买超卖）
- 形态识别（头肩底/双底/三角形等，后期加）

### 5.3 债券市场模块

- 国债收益率曲线图（1年/3年/5年/10年/30年）
- 美债收益率对比
- 中美利差
- LPR/Shibor/MLF 利率时间线
- 可转债行情

### 5.4 期货/大宗商品模块

- 国内期货行情表（主力合约）
- 国际大宗商品（黄金/原油/铜/天然气）
- 期现基差
- 期权 T 型报价（50ETF/300ETF）

### 5.5 加密货币模块

- BTC/ETH 实时行情（Binance WS 直推）
- 主流币行情表
- 恐惧贪婪指数
- DeFi TVL 排行
- 大盘总市值/24h成交量

### 5.6 预测市场模块（Polymarket）

- 热门预测市场列表及实时概率
- 按类别筛选（政治/经济/科技/体育/加密）
- 概率变化时间线
- 交易量排行

### 5.7 宏观数据模块

- 中国宏观经济仪表盘（GDP/CPI/PMI/M2/社融）
- 美国宏观经济仪表盘（就业/CPI/FOMC）
- 央行利率表
- 经济日历（即将发布的数据）

### 5.8 AI 动向模块

- AI 资讯流（RSS 聚合 + AI 摘要）
- 重要论文卡片（arXiv + Papers With Code）
- GitHub Trending AI 仓库
- Hugging Face 新模型/数据集
- 大模型 API 价格变动跟踪
- AI 融资/政策动态时间线

### 5.9 其他

- **自选监控**：跨品类自选列表（可选股票+币+期货混排）
- **告警系统**：价格突破、涨跌幅阈值、技术信号等条件告警（浏览器通知 + 可能推微信/飞书）
- **搜索**：全品类证券搜索
- **深色主题**：Bloomberg 风格的暗色终端感 UI

---

## 六、部署方案

### 6.1 方案：Windows 本地 Docker Desktop

```
你的 Windows 电脑（已有翻墙环境）
  ├── Docker Desktop for Windows（WSL2 后端）
  ├── Docker Compose 一键启动全部服务
  ├── PostgreSQL + TimescaleDB + Redis 本地运行
  ├── 前端 localhost:5173（Vite 开发服务器）
  ├── 后端 localhost:8000（FastAPI）
  └── 翻墙：本机 Clash/V2Ray 代理，采集层配置 HTTP_PROXY 走代理
```

**为什么不用 VPS**：你电脑本身能翻墙，境外 API 直接走本地代理。所有服务都在本机跑，零延迟、零月费、数据完全本地。唯一代价是电脑不能关机（或至少在交易时段不能关）。

### 6.2 Docker Compose 服务编排

```yaml
# docker-compose.yml
services:
  postgres:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_DB: finboard
      POSTGRES_USER: finboard
      POSTGRES_PASSWORD: ...
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]

  api:
    build: ./backend
    ports: ["8000:8000"]
    depends_on: [postgres, redis]
    environment:
      DATABASE_URL: postgresql://...
      REDIS_URL: redis://redis:6379
      # 翻墙代理：宿主机上 Clash 的端口
      HTTP_PROXY: http://host.docker.internal:7890
      HTTPS_PROXY: http://host.docker.internal:7890

  worker:
    build: ./backend
    command: celery -A app.celery worker -l info
    depends_on: [postgres, redis]
    environment:
      HTTP_PROXY: http://host.docker.internal:7890
      HTTPS_PROXY: http://host.docker.internal:7890

  beat:
    build: ./backend
    command: celery -A app.celery beat -l info
    depends_on: [postgres, redis]

volumes:
  pgdata:
```

> `host.docker.internal` 是 Docker Desktop 访问宿主机网络的标准方式。你的 Clash 开在 7890 端口，容器内就能走代理。

### 6.3 前端开发与启动

前端用 Vite 开发服务器直接跑在宿主机（不放在 Docker 里，方便热更新）：

```bash
cd frontend
npm install
npm run dev        # localhost:5173
```

### 6.4 浏览器抓取方案（兜底策略）

有些数据源没有标准 API 或 API 不稳定时，用有头/无头浏览器直接抓页面：

```
数据源                       抓取方式
─────────────────────────────────────────────
Polymarket            Playwright 打开 polymarket.com，抓 market 列表和价格
东方财富板块数据        API 优先，若反爬升级则 Playwright 兜底
Investing.com 经济日历  Playwright 抓日历页面
AI 资讯（无 RSS 源）     Playwright 打开页面 + readability 提取正文
央行官网公告            Playwright 抓取 MLF/逆回购操作公告
Twitter/X AI 动态       用 browser tool 或 Playwright 抓取
```

技术栈：
- **Playwright**（Python 版）：比 Selenium 快、API 更现代、自动管理浏览器驱动
- **beautifulsoup4 + lxml**：HTML 解析
- **readability-lxml**：从网页中提取正文（像 Safari 阅读模式）

示例代码骨架：

```python
# collectors/browser_scraper.py
from playwright.async_api import async_playwright

async def scrape_polymarket_markets():
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)
        page = await browser.new_page()
        await page.goto("https://polymarket.com/markets")
        # 等待市场卡片渲染
        await page.wait_for_selector("[data-testid='market-card']")
        markets = await page.evaluate("""
            () => Array.from(document.querySelectorAll('[data-testid="market-card"]'))
                .map(card => ({
                    title: card.querySelector('a').textContent,
                    probability: card.querySelector('[data-testid="probability"]').textContent
                }))
        """)
        await browser.close()
        return markets
```

> Playwright 可以复用浏览器的已登录状态（Cookie/Session），避免被反爬。首次使用时手动登录一次，后续自动读取。

---

## 七、实施路线图

### Phase 1 — 核心骨架（2-3 周）

- [ ] 项目初始化：Docker Compose、Python 环境、React 脚手架
- [ ] 数据库建表（PostgreSQL + TimescaleDB）
- [ ] FastAPI 基础框架 + REST API
- [ ] 前端基础布局（Grid 看板 + 深色主题）
- [ ] AKShare 接入：A 股实时行情 + 日线数据采集
- [ ] Celery Beat 定时任务框架搭建
- [ ] 第一版 A 股行情展示（指数 + 个股 K 线）

### Phase 2 — A 股深度（2-3 周）

- [ ] 涨停板/跌停板数据采集与展示
- [ ] 热门板块/概念板块
- [ ] 龙虎榜
- [ ] 技术指标计算与图表叠加（MA/MACD/KDJ/RSI/BOLL）
- [ ] 分时图
- [ ] 资金流向

### Phase 3 — 国际化扩展（2 周）

- [ ] 美股模块（yfinance）
- [ ] 加密货币模块（Binance WS + CoinGecko）
- [ ] 全球指数一览
- [ ] WebSocket 实时推送架构

### Phase 4 — 补齐品类（2 周）

- [ ] 债券市场模块
- [ ] 期货/大宗商品模块
- [ ] 宏观经济模块
- [ ] 汇率模块
- [ ] 经济日历
- [ ] Polymarket 预测市场

### Phase 5 — AI 动向 + 智能化（2 周）

- [ ] AI 资讯 RSS 聚合抓取
- [ ] AI 摘要生成（LLM API 调用）
- [ ] arXiv/HF/GitHub 追踪
- [ ] 大模型 API 价格跟踪

### Phase 6 — 完善与迭代（持续）

- [ ] 自选监控 + 告警系统
- [ ] 搜索功能
- [ ] 移动端适配/PWA
- [ ] 形态识别等高级技术分析
- [ ] 桌面端打包（可选）

---

## 八、关键风险与对策

| 风险 | 对策 |
|------|------|
| AKShare 接口不稳定 | 多数据源备份（新浪 `hq.sinajs.cn` / 腾讯 `qt.gtimg.cn` / 东方财富 JSON API 相互切换） |
| 免费 API 频率限制 | 本地缓存 + 合理轮询间隔 + Redis 去重 |
| yfinance 被限速 | 延迟足够即可，美股仅需收盘数据，盘中不需要秒级刷新 |
| 翻墙代理波动 | 本地 Clash 常驻运行；国内数据直连不受影响 |
| Polymarket/网页数据无 API | Playwright 浏览器抓取兜底，反正你有翻墙和浏览器 |
| 数据量大导致查询慢 | TimescaleDB 自动分区（按天）+ Redis 热数据缓存 |
| 前端实时刷新压力 | WebSocket 增量推送，不做全量轮询 |
| 电脑关机/休眠 | 交易时段保持运行即可；盘后/周末无所谓 |
| Docker Desktop 内存占用 | WSL2 分配 4-6GB，其余 Windows 正常用 |

---

## 九、成本估算

| 项目 | 费用 |
|------|------|
| 服务器 | **￥0**（本机 Windows 运行） |
| 数据库/缓存 | **￥0**（Docker Desktop 本地） |
| 金融数据源 | **￥0**（AKShare + yfinance + Binance WS + CoinGecko 等全部免费） |
| LLM API（AI 资讯摘要） | **￥5-20/月**（DeepSeek API 极便宜，每天几百条新闻摘要几毛钱） |
| 翻墙代理 | **你已有** |
| 域名 | **不需要**（localhost:5173 本地访问） |
| **合计** | **约 ￥10-30/月**（仅仅是 LLM 摘要费用） |

> 如果 LLM 用本地 Ollama 跑（如 qwen2.5），完全零成本。只需要花电费。

---

## 十、确认事项

1. **Docker Desktop 装好了吗？** Windows 上需要 WSL2 + Docker Desktop，这是整个部署的基础
2. **代理端口确认**：你的 Clash/V2Ray 代理端口是不是 7890？（不一定是这个端口，需要确认）
3. **AI 摘要用哪个模型**：DeepSeek API（最便宜）vs 本机 Ollama（零成本）vs OpenAI（效果最好）？
4. **告警要不要打通微信/飞书**？可以和已有的 Bot 体系对接
5. **自选监控偏好**：先列一下你最关注的标的（股票代码、板块、币种等），Phase 1 直接内置进去

> 方案就绪。你确认方向后，我从 Phase 1 开始搭建。第一个目标：一周内让 A 股行情跑起来。
